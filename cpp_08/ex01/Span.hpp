#include <iostream>

class Span
{
	private:
		unsigned int	N;
		std::span<T> 		S
	public:
		Span(unsigned in N);
		Span(const Span& other);
		Span& operator = (const Span& other);
		~Span();


		addNumber();
		shortestSpan();
		longestSpan();
}
